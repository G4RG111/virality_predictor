# Deploy requirements — excludes torch/sentence-transformers (~2GB) and postgres drivers
# SQLite is used instead of PostgreSQL in production (DATABASE_URL=sqlite:///./vibe.db)

# FastAPI stack
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database (SQLite via SQLAlchemy — no asyncpg/psycopg2/pgvector needed)
sqlalchemy>=2.0.0
alembic>=1.12.0

# Config
pydantic>=2.4.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Data / ML (lightweight path — use_ml_model=False so XGBoost never runs)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
shap>=0.43.0

# PDF parsing
pdfplumber>=0.10.0

# Web scraping / social signals
beautifulsoup4>=4.12.0
lxml>=5.0.0
httpx>=0.25.0
pytrends>=4.9.2

# Utilities
structlog>=23.1.0
